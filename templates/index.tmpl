<html>
    <head>
        <title>{{ .title }}</title>
        <meta charset="UTF8" />
    </head>
    <body>
        <ul>

            {{range $i, $v := .news_list}}
             <li>{{ $v.title }}</li>
            {{end}}
        </ul>
    </body>
</html>